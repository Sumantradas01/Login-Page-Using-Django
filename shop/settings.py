# ...existing code...

# Redirect URLs after login/logout
LOGIN_REDIRECT_URL = '/'
LOGOUT_REDIRECT_URL = '/'

# Ensure the database is set up for the default User model
# (Django's default settings already include this)
# AUTH_USER_MODEL = 'auth.User'